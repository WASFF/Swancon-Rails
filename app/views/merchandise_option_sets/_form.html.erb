<%= form_for(@merchandise_option_set) do |f| %>
  <% if @merchandise_option_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@merchandise_option_set.errors.count, "error") %> prohibited this merchandise_option_set from being saved:</h2>

      <ul>
      <% @merchandise_option_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
