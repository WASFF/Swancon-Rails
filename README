Todo:

content blocks should be able to have (optional) ContentImages as a header. 
Use above to make guests section work properly

Content blocks should have dates? Optionally have a specialized content block that is an event?

Add program item request thingy to the front page.
