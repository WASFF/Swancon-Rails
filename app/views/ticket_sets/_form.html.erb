<%= form_for(@ticket_set) do |f| %>
  <% if @ticket_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_set.errors.count, "error") %> prohibited this ticket_set from being saved:</h2>

      <ul>
      <% @ticket_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
