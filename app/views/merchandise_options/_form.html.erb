<%= form_for([@merchandise_option.merchandise_type, @merchandise_option]) do |f| %>
  <% if @merchandise_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@merchandise_option.errors.count, "error") %> prohibited this merchandise_option from being saved:</h2>

      <ul>
      <% @merchandise_option.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :merchandise_type_id %> <%= @merchandise_option.merchandise_type.name %>
  </div>
  <div class="field">
    <%= f.label :merchandise_option_set_id, "Merchandise Option Set" %><br />
		<%= f.select(:merchandise_option_set_id, MerchandiseOptionSet.order(:name).all.collect {|item| [item.name, item.id]}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
